Set

Unordered, unqiue values, immutable


set1 = {1, 2, 3, 4, 5}

# Add an element
set1.add(6)  
print(set1)  # {1, 2, 3, 4, 5, 6}

# Remove an element (KeyError if not found)
set1.remove(3)  
print(set1)  # {1, 2, 4, 5, 6}

# Discard an element (no error if not found)
set1.discard(10)  
print(set1)  # unchanged

# Pop an element (removes and returns a random item)
item = set1.pop()
print("Popped:", item)
print(set1)
